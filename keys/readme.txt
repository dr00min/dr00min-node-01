Put here the SSL's certificate keys.
======================================================================

service.crt - fulchain public key
service.key - private key

One SSL certificates provider is https://www.ssls.com

The HTTPS server (web443.js) creates self signed certificates by default when none exist.

